apiVersion: v1
kind: Service
metadata:
  name: teamops-ui
  namespace: teamops
  labels:
    app: teamops-ui
spec:
  type: ClusterIP
  selector:
    app: teamops-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
